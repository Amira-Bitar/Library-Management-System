const {{pascalCase name}} = require("../models/{{pascalCase name}}");

// Create
exports.create{{pascalCase name}} = async (req, res) => {
  try {
    const new{{pascalCase name}} = await {{pascalCase name}}.create(req.body);

    res.status(201).json(new{{pascalCase name}});
  } catch (error) {
    res.status(500).json({ message: error.message });
  }
};

// Get all
exports.getAll{{pascalCase name}}s = async (req, res) => {
  try {
    const data = await {{pascalCase name}}.find();

    res.status(200).json(data);
  } catch (error) {
    res.status(500).json({ message: error.message });
  }
};

// Get one
exports.get{{pascalCase name}}ById = async (req, res) => {
  try {
    const data = await {{pascalCase name}}.findById(req.params.id);

    res.status(200).json(data);
  } catch (error) {
    res.status(500).json({ message: error.message });
  }
};

// Update
exports.update{{pascalCase name}} = async (req, res) => {
  try {
    const updated = await {{pascalCase name}}.findByIdAndUpdate(
      req.params.id,
      req.body,
      { new: true }
    );

    res.status(200).json(updated);
  } catch (error) {
    res.status(500).json({ message: error.message });
  }
};

// Delete
exports.delete{{pascalCase name}} = async (req, res) => {
  try {
    await {{pascalCase name}}.findByIdAndDelete(req.params.id);

    res.status(200).json({
      message: "{{pascalCase name}} deleted successfully",
    });
  } catch (error) {
    res.status(500).json({ message: error.message });
  }
};