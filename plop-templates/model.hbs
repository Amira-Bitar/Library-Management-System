const mongoose = require("mongoose");

const {{pascalCase name}}Schema = new mongoose.Schema(
  {

  },
  { timestamps: true }
);

module.exports = mongoose.model(
  "{{pascalCase name}}",
  {{pascalCase name}}Schema
);